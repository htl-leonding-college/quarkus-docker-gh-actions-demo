package at.htl.vehicle.boundary;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeVehicleEndpointIT extends VehicleEndpointTest {

    // Execute the same tests but in native mode.
}